# Artifact publication boundary

Only explicitly public synthetic build assets belong here. No credentials, private source, Cloud captures, customer records or feedback. Preserve every committed public release/manifest path. Use the platform's versioned Pages stage/check/verify commands; new releases have new content addresses. Verify old and new URLs after every publication. Do not enable protected delivery or change an existing frozen review URL here.
